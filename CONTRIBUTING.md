# Contributing

1. Create a branch from `main`.
2. Update or add Markdown under `docs/`.
3. Run `npm run build` before opening a pull request.
4. Add an ADR for significant architectural changes.
5. Require at least one product and one engineering reviewer for cross-functional changes.
